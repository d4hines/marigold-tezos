# This contract intends to call "opcodes/ordering_mix_dfs_bfs_parent.tz".
#
# Given a list, each elements of the list present the way to operation "opcodes/ordering_mix_dfs_bfs_parent.tz"
#
# list (pair
#         (pair
#             # This list in the input of "ordering_mix_dfs_bfs_parent.tz"
#             # For more details, please see "ordering_mix_dfs_bfs_parent.tz"
#             (list (pair (pair string address) (pair bool bool)))
#
#             # The address is the address of ordering_mix_dfs_bfs_parent.tz
#             address)
#
#         # The first bool is to control the order of in BFS(True) or DFS(False) by ALLOW_DFS
#         # The second bool is to offer permission to run DFS by `ALLOW_DFS`
#         (pair bool bool))
#

{ parameter
    (list (pair (pair (list (pair (pair string address) (pair bool bool))) address) (pair bool bool))) ;
  storage unit ;
  code { NIL operation ;
         SWAP ;
         CAR ;
         ITER { DUP ;
                CDR ;
                SWAP ;
                CAR ;
                DUP ;
                CDR ;
                SWAP ;
                CAR ;
                DIG 2 ;
                DUP ;
                CDR ;
                SWAP ;
                CAR ;
                DIG 3 ;
                CONTRACT (list (pair (pair string address) (pair bool bool))) ;
                IF_NONE { PUSH string "Not a contract" ; FAILWITH } {} ;
                SWAP ;
                IF { SWAP ;
                     IF { DIG 2 ; SWAP ; PUSH mutez 0 ; DIG 3 ; TRANSFER_TOKENS ; ALLOW_DFS ; CONS }
                        { DIG 2 ; SWAP ; PUSH mutez 0 ; DIG 3 ; TRANSFER_TOKENS ; CONS } }
                   { SWAP ;
                     IF { DIG 2 ; SWAP ; PUSH mutez 0 ; DIG 3 ; TRANSFER_TOKENS ; MAKE_DFS ; ALLOW_DFS ; CONS }
                        { DIG 2 ; SWAP ; PUSH mutez 0 ; DIG 3 ; TRANSFER_TOKENS ; MAKE_DFS ; CONS } } } ;
         NIL operation ;
         SWAP ;
         PAIR ;
         LEFT (list operation) ;
         LOOP_LEFT
           { DUP ;
             CDR ;
             SWAP ;
             CAR ;
             IF_CONS
               { DIG 2 ; SWAP ; CONS ; SWAP ; PAIR ; LEFT (list operation) }
               { RIGHT (pair (list operation) (list operation)) } } ;
         PUSH unit Unit ;
         SWAP ;
         PAIR } }

