parameter unit;
storage unit;
code { DROP;

       # Log a packable type
       PUSH string "dummy";
       PACK;
       LOG;

       # int
       PUSH int 1;
       LOG;

       # lambda
       PUSH (lambda unit unit) { };
       LOG;

       # map
       EMPTY_MAP string int;
       LOG;
       
       UNIT; NIL @noop operation; PAIR; };
